from cs50 import get_int

while True:
    print("Number:", end="")
    creditcard = get_int()
    if(creditcard >= 0):
        break

check(creditcard)

check(cardnum)
    length = get_length
    if (checklength(length)) == True
